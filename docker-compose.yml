services:
  haoyu-ai:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: haoyu-ai-interview-agent

    ports:
      - "8000:8000"

    env_file:
      - .env

    environment:
      PYTHONUNBUFFERED: "1"

    volumes:
      - ./data:/app/data

    restart: unless-stopped